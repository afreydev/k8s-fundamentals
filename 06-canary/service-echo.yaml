apiVersion: v1
kind: Service
metadata:
  name: echo-service
spec:
  type: NodePort
  selector:
    app: echo          # matches BOTH versions — version label is NOT here
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5678
      nodePort: 30090
